kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: productpage
spec:
  to:
    kind: Service
    name: productpage
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect